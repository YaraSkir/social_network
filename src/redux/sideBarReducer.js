let initialState = [{name: 'Alex'}, {name: 'Alisa'}, {name: 'Andrey'}];

const sideBarReducer = (state = initialState, action) => {
   // let copyState = {...state};
    return state;
}

export default sideBarReducer;